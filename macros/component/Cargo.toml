[package]
name = "component"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true

[dependencies]
# stable_component_id = { path = "../stable_component_id" }
syn = { version = "2.0", features = ["full"] }
quote = "1.0"
serde = { version = "1.0", features = ["derive"] }
