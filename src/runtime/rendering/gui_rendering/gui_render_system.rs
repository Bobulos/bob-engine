use crate::runtime::ecs::SystemBase;

pub struct GuiRenderSystem {
    
}

impl GuiRenderSystem {
    
}

impl SystemBase for GuiRenderSystem {
    fn on_start(&mut self, world: &std::sync::Arc<crate::runtime::ecs::DynamicWorld>) {
        
    }
    fn on_update(&mut self, world: &std::sync::Arc<crate::runtime::ecs::DynamicWorld>) {
        
    }
    fn on_destroy(&mut self, world: &std::sync::Arc<crate::runtime::ecs::DynamicWorld>) {
        
    }
}