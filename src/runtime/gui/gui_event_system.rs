use crate::runtime::ecs::SystemBase;

pub struct GuiEventSystem {
    
}

impl SystemBase for GuiEventSystem {
    fn on_start(&mut self, _world: &std::sync::Arc<crate::runtime::ecs::DynamicWorld>) {
        
    }
    fn on_update(&mut self, _world: &std::sync::Arc<crate::runtime::ecs::DynamicWorld>) {
        
    }
    fn on_destroy(&mut self, _world: &std::sync::Arc<crate::runtime::ecs::DynamicWorld>) {
        
    }
}
impl GuiEventSystem {
    
}